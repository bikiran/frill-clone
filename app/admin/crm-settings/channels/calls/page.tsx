// The Calls & SMS UI moved to /admin/integrations/calls; re-export it here so
// the CRM-settings channel link keeps rendering the same page in place.
export { default } from '@/app/admin/integrations/calls/page'
